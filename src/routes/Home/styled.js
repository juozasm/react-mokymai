import styled from 'styled-components'
import { Container as StyledContainer } from '../../styled/layout'

export const Container = styled(StyledContainer)`
    background-color: #FDDDDD;
    padding: 100px;
`
